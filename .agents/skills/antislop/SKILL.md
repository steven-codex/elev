---
name: antislop
description: Anti-slop quality filter rules for AI coding agents to eliminate generic AI-generated UI designs, repetitive template layouts, fake marketing copy, and useless code comments.
---

# antislop

> Anti Slop: Rules for AI Coding Agents
> Follow these rules whenever generating or building UI for a website, web app, or any interface.
> The goal: the design should feel **crafted by a designer**, not generated by AI.

---

## Core Principle

The filter rejects technique without purpose, not technique itself. Before using any visual technique, answer: **what does this serve?** If the only answer is "it looks AI" or "it looks safe", the technique must go or be reworked. If the answer names a hierarchy, identity, or readability goal, it stays.

The question to answer before calling anything done:
> If the logo and product name were swapped out, would this design still feel unique and have its own character?

If the answer is **no**, the design is too generic.

---

## The Craftsmanship Standard

### C-1 — Intentionality
Every visual and copy decision has a reason you can articulate. If the only reason is "it's the AI default", that is a red flag: revisit the decision.

### C-2 — Functional Completeness
Every interactive element works, or it does not exist. A button that cannot do anything is a defect, not decoration.

### C-3 — Content-Driven Composition
Every section exists because the product's content needs it, not because every AI landing page has one. Remove sections that only fill a template.

### C-4 — Resilience
The UI holds up in every state (empty, loading, error), every theme you ship, every breakpoint, and keyboard-only use.

### C-5 — Evidence Over Claims
Anything presented as fact (testimonials, statistics, security claims) is real and verifiable, or it is not shown at all.

---

## Hard Gate Rules (Non-Negotiable)

- **R-01 No Default Blue-Purple Glow Gradients**: Do not wrap whole pages in generic blue-to-purple background blobs unless specifically branded.
- **R-02 No Generic Floating Bento Card Slop**: Avoid copy-pasted 3-column card layouts where every card has the same white box, same border, and same Lucide icon.
- **R-03 No Fake Social Proof or Artificial Claims**: Do not invent fake user counts ("Loved by 50,000+ teams") or fake logos.
- **R-04 Editorial Typography First**: Avoid 6-line title wraps. Use expressive headline sizes, tight line-heights, and curated typography pairs.
- **R-05 Interactive Tactile Feedback**: All buttons and clickable elements must respond with spring physics or active transforms (`active:scale-[0.96]`).
- **R-06 No AI Code Comment Slop**: Never generate useless explanatory comments like `// Render container div` or `// Return JSX`. Only write comments explaining non-obvious business logic.
